.. _file-based:

File Routing
============

A single file ``routing.cfg`` or ``routing.py`` defines the mapping
between a URL and a function.

This is similar to the Django method of routing using ``urls.py``.
